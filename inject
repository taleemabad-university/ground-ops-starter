#!/usr/bin/env bash
# The day-2 failure injector. ./inject --help for the list.
set -uo pipefail
cd "$(dirname "$0")"
PY="${PYTHON:-}"
if [ -z "$PY" ]; then
  command -v python3 >/dev/null 2>&1 && PY=python3 || PY=python
fi
exec "$PY" -m harness.inject "$@"
